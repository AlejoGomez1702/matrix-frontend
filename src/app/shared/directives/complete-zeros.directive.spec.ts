import { CompleteZerosDirective } from './complete-zeros.directive';

describe('CompleteZerosDirective', () => {
  it('should create an instance', () => {
    const directive = new CompleteZerosDirective();
    expect(directive).toBeTruthy();
  });
});
